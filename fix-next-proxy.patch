diff --git a/next.config.js b/next.config.js
index 2ae6cc3..d2e3b55 100644
--- a/next.config.js
+++ b/next.config.js
@@ -1,12 +1,27 @@
 /* eslint-disable no-param-reassign */
 module.exports = {
   async rewrites() {
-    return [
-      {
-        source: '/api/:path*',
-        destination: 'https://api.example.com/:path*',
-      },
-    ];
+    // NOTE:
+    // This app implements its own Next.js API routes under /api/llm/*.
+    // Do NOT proxy those routes.
+    //
+    // If you want to proxy other /api/* calls to a separate backend,
+    // set API_PROXY_BASE (e.g. https://your-backend.example.com).
+    const proxyBase = process.env.API_PROXY_BASE;
+
+    const rules = [
+      // Keep internal LLM routes local (prevents accidental proxying in dev).
+      { source: '/api/llm/:path*', destination: '/api/llm/:path*' },
+    ];
+
+    if (proxyBase) {
+      rules.push({
+        source: '/api/:path*',
+        destination: `${proxyBase}/:path*`,
+      });
+    }
+
+    return rules;
   },
   webpack(config) {
     config.module.rules.push({
       test: /\.svg$/,
       use: ['@svgr/webpack'],
